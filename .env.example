# FieldLens — environment variable template
# This file is a placeholder for Day 1. Real variables (DATABASE_URL, JWT
# secrets, MinIO credentials, etc.) will be added on Day 2 when Postgres and
# Prisma are set up.
#
# Rule: this file is committed. The actual .env is NEVER committed.
